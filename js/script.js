AOS.init({
  duration: 900,
  once: true,
  easing: 'ease-in-out'
});

GLightbox({
  touchNavigation: true,
  loop: true,
  zoomable: true
});

AOS.init({duration:900,once:true});
GLightbox({loop:true,zoomable:true});
